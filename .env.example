# This site is deployed as a static Vite artifact behind Nginx.
# Runtime lead capture is proxied through /api/lead in nginx.conf.

# Add future client-side tracking IDs only when needed.
VITE_GA4_MEASUREMENT_ID=""
VITE_GTM_CONTAINER_ID=""
VITE_META_PIXEL_ID=""
